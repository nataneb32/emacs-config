(in-package #:people-marketing-api.db)

(connect-toplevel :sqlite3 :database-name ":MEMORY:")

(deftable lead ()
  ((name :col-type (:varchar 64))
   (email :col-type (:varchar 64))
   (phone :col-type (:varchar 64))))

(ensure-table-exists 'lead)
